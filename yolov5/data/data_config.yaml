# parent
# └── yolov5
#     └── data
#         └── data_config.yaml

# Paths
train: ../../../datasets/cones/images/train  # path to the training images
val: ../../../datasets/cones/images/validation  # path to the val images
test: ../../../datasets/cones/images/test  # path to the testing images

# Classes
nc: 5  # number of classes
names: ['blue cone', 'orange cone', 'red cone', 'mystery cone', 'yellow cone'] # class names, indices match label numbers
                                                # what color is 'mystery cone'??
